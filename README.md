# desktop-setup

This assumes you are using Fedora

## Things to fix

- You need to install pip and git
- Krew install isn't working properly
  - Need to install kubectl first and ensure in PATH
  - See https://krew.sigs.k8s.io/docs/user-guide/setup/install/ for "new install"
- Dotfiles didn't copy (zshrc and p10k.zsh)

## Future additions

- Add more customizations for desktop (ex. KDE configuration)
- Authentication to GitHub to avoid rate limiting
